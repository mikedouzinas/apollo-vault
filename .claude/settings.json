{
  "permissions": {
    "defaultMode": "acceptEdits"
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "grep -q '^## FIRST RUN' CLAUDE.md 2>/dev/null && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"SessionStart\",\"additionalContext\":\"\\n\\nThis vault has not been set up yet (CLAUDE.md still contains its FIRST RUN section). Follow the FIRST RUN section of CLAUDE.md now: greet them, explain in plain words what this folder is, ask the four questions one at a time, and write their answers into CLAUDE.md in place of that whole section. Replacing that section is what marks setup complete, so do not leave it in the file. Then invite them to give you something to read. Do not tell them to run a slash command.\"}}' || true"
          }
        ]
      }
    ]
  }
}
